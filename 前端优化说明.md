# 前端优化说明

> 本文档汇总前端可优化点，按 **由简到难 + 依赖关系** 排序。每项标注：
> - **难度**：★（纯样式/配置）～★★★★★（涉及状态机/同步逻辑）
> - **依赖**：需要哪些前置优化先完成
> - **风险**：改动可能影响的范围
>
> 建议从上到下逐项实施；遇到标有「依赖」的项目需先完成依赖项。

---

## P1 — 简单样式 / 配置类（低风险，可立即开工）

### 1.1 卡牌悬停升起动画统一 ★
**问题**：`.card:hover` 直接使用 `transform: translateY(-14px)`，但 `GameBoard.tsx` 中又用 JS 计算手牌布局（`computeCardLayouts` 实现 Dock 风格放大），CSS hover 与 JS 布局 transform 会叠加冲突。
**方案**：移除 `.card:hover` 的 `translateY`，统一由 JS layout 计算 `liftY`。
**依赖**：无
**风险**：低，仅视觉表现
**文件**：`styles.css` L619-621

### 1.2 弃牌堆顶牌切换动画 ★
**问题**：当有玩家打出牌时，`.discard` 内容瞬间替换，没有过渡动画。
**方案**：给 `.discard` 加 `transition: transform 0.2s ease`；在 React 层为新顶牌挂一个 `key`，触发 CSS keyframe 动画（从 `scale(1.15)` 缩到 `1`）。
**依赖**：无
**风险**：低
**文件**：`GameBoard.tsx`、`styles.css`

### 1.3 手牌数变化时徽章脉冲 ★
**问题**：`.own-hand-count` 和 `.hand-count-badge` 数字变化时没有视觉反馈。
**方案**：监听 `hand.length` 变化，给徽章加一个 0.3s 的 `pulse` 动画 class。
**依赖**：无
**风险**：低
**文件**：`GameBoard.tsx`、`styles.css`

### 1.4 对手离线标识优化 ★
**问题**：`.opponent.disconnected` 仅降低 opacity 到 0.55，辨识度低。
**方案**：在对手头像旁加一个"已离线"小图标（灰色圆点+文字 tooltip）。
**依赖**：无
**风险**：低
**文件**：`GameBoard.tsx`、`styles.css`

### 1.5 日志面板滚动行为 ★
**问题**：`.log-content` 新增日志时，若用户向上翻阅历史，会被自动滚回底部。
**方案**：在 `logRef` 的 `useEffect` 中检查"用户是否在底部附近"（`scrollHeight - scrollTop - clientHeight < 50`），仅在底部时才自动滚。
**依赖**：无
**风险**：低
**文件**：`GameBoard.tsx`

### 1.6 按钮 disabled 态可点击提示 ★
**问题**：`.card.disabled` 的牌 `cursor: not-allowed`，但用户不知道为什么不能出。
**方案**：hover disabled 牌时显示 tooltip 说明原因（"当前处于罚摸连锁"/"颜色不匹配"/"非当前回合"等）。
**依赖**：无
**风险**：低
**文件**：`GameBoard.tsx`、`styles.css`

---

## P2 — 中等难度（需要状态/事件配合）

### 2.1 回合切换提示动画 ★★
**问题**：回合切换时仅靠 `.opponent.active` 边框变化提示，不够醒目。
**方案**：新回合玩家头像加 0.5s 的 `glow` 脉冲动画（金色外圈扩散）。同时考虑在桌面中央显示"XX 的回合"1s 浮层。
**依赖**：无
**风险**：中（需监听 `currentPlayerIndex` 变化）
**文件**：`GameBoard.tsx`、`gameStore.ts`、`styles.css`

### 2.2 加牌链累积时的视觉警示 ★★
**问题**：`drawCardStack > 0` 时仅在弃牌堆右上角显示 `.penalty-count`，玩家容易忽略自己即将承受大量罚牌。
**方案**：
- 当 `drawCardStack >= 4` 时，给 `.penalty-count` 加 `shake` 动画
- 当 `drawCardStack >= 8` 时，在即将承受罚摸的玩家区域加红色边框脉冲
- 顶栏显示"⚠️ 加牌链：N"提示条
**依赖**：无
**风险**：中（需判断"即将承受罚摸的玩家"是当前玩家还是连锁源）
**文件**：`GameBoard.tsx`、`styles.css`

### 2.3 ColorModal 改色按钮放大预览 ★★
**问题**：`ColorModal` 选色时只能看到四个圆圈，选完后牌面颜色才变化。
**方案**：hover 某色时，wild 牌预览同步变色（目前已有 `combo-preview` 机制，可复用）。
**依赖**：无
**风险**：低
**文件**：`ColorModal.tsx`、`GameBoard.tsx`

### 2.4 倒计时进度条可视化 ★★
**问题**：`.phase-time` 仅显示剩余秒数数字，没有进度条，紧迫感不足。
**方案**：在 `.phase-panel` 底部加一条细进度条，宽度从 100% 渐变到 0%；最后 3 秒变红+脉冲。
**依赖**：无
**风险**：中（需从服务端获取 `phaseDurations` 或从 state 推算）
**文件**：`GameBoard.tsx`、`styles.css`

### 2.5 摸牌堆耗尽时的视觉提示 ★★
**问题**：`drawPileCount` 低时没有提示。
**方案**：`drawPileCount <= 10` 时 `.deck` 边框变红；`<= 3` 时加 `shake` 动画 + "牌堆即将耗尽" toast。
**依赖**：无
**风险**：低
**文件**：`GameBoard.tsx`、`styles.css`

### 2.6 队友手牌展开优化（触屏） ★★
**问题**：`.teammate-hand-wrapper:hover` 在触屏设备上无法触发。
**方案**：改为点击切换展开/折叠，保留 hover 作为桌面端预览。
**依赖**：无
**风险**：中（需新增 state 管理展开状态）
**文件**：`GameBoard.tsx`

---

## P3 — 较高难度（涉及多模块协作）

### 3.1 出牌飞行动画 ★★★
**问题**：玩家出牌后，牌瞬间从手牌区消失、出现在弃牌堆，没有飞行动画。
**方案**：
- 出牌瞬间，在原位置 clone 一张卡牌 DOM
- 用 `FLIP` 动画技术：记录起止位置，用 transform 让它"飞"到弃牌堆位置
- 飞行途中加旋转 + 缩放，落定时回弹
**依赖**：1.2（弃牌堆顶牌切换动画）—可合并实现
**风险**：高（FLIP 动画需要精确的 getBoundingClientRect 计算 + 时机控制）
**文件**：`GameBoard.tsx`、`styles.css`

### 3.2 摸牌动画 ★★★
**问题**：玩家摸牌后，新牌直接 append 到手牌末尾，没有从牌堆"飞过来"的效果。
**方案**：摸牌时，在 `.deck` 位置 clone 一张牌背，飞到手牌区末尾位置，到达后翻面显示牌面。
**依赖**：3.1（共用 FLIP 工具函数）
**风险**：高（需考虑摸牌后 `playableDrawnCardId` 的展示逻辑）
**文件**：`GameBoard.tsx`、`styles.css`

### 3.3 罚摸连锁的牌"雨"动画 ★★★
**问题**：`applyPassTurn` 一次性摸 N 张时，手牌瞬间 +N，没有视觉过程。
**方案**：
- 当 `drawCardStack >= 4` 时，N 张牌从牌堆连续飞到承受者位置
- 每张牌间隔 80ms 出发，整体动画约 `N * 80 + 300ms`
- 配合音效"唰唰唰"
**依赖**：3.2（摸牌动画）
**风险**：高（动画期间 UI 不可阻塞操作；需要服务端配合延迟或客户端纯视觉层做）
**文件**：`GameBoard.tsx`、`styles.css`、`audio.ts`

### 3.4 抢牌（snatch）高亮优化 ★★★
**问题**：`can-snatch` 状态仅在可抢牌上加 `box-shadow`，玩家不知道抢牌的时机和目标。
**方案**：
- snatch_window 阶段，所有可抢的牌加 `can-snatch` + 0.5s 闪烁
- 在屏幕顶部显示"抢牌阶段：X 秒"倒计时条
- 当玩家选中 snatch 牌后，弃牌堆高亮显示"放入位置"指示
**依赖**：2.4（倒计时进度条）
**风险**：中
**文件**：`GameBoard.tsx`、`styles.css`

### 3.5 焦点动画优化（爆牌/喊UNO/出完牌） ★★★
**问题**：当前 `focusTarget` 通过 CSS `transform: scale + translate` 实现，scale 固定 1.6，但不同屏幕尺寸下视觉效果不一致；`player_then_discard` 两段动画时长硬编码。
**方案**：
- scale 根据视口动态计算（`min(viewportWidth / targetWidth, 1.6)`）
- 动画时长提到 `gameStore` 的常量配置区，方便调优
- 爆牌动画（`burstCenterAnim`）增加震动效果（`shake` 关键帧）
**依赖**：无
**风险**：中（改动现有动画逻辑，需回归测试三种触发场景）
**文件**：`GameBoard.tsx`、`gameStore.ts`、`styles.css`

---

## P4 — 高难度（涉及状态机重构 / 性能优化）

### 4.1 手牌区响应式布局重构 ★★★★
**问题**：
- `grid-template-rows: 150px 1fr 230px` 固定三行高度，小屏幕下手牌区挤压
- `computeCardLayouts` 用 `CARD_WIDTH = 80` 硬编码，不响应 `--card-width` 变量
- 6 人模式时对手区可能溢出
**方案**：
- 用 CSS Grid `minmax()` + `clamp()` 让三行高度根据手牌数动态调整
- `computeCardLayouts` 读取 CSS 变量或通过 `ResizeObserver` 动态获取 card width
- 6 人模式时对手区改为 3×2 网格而非单行 flex
**依赖**：无
**风险**：高（改动影响所有模式的所有屏幕尺寸）
**文件**：`GameBoard.tsx`、`styles.css`

### 4.2 手牌区拖拽优化 ★★★★
**问题**：当前 `.card.dragging` 仅降低 opacity，没有真正的拖拽重排。
**方案**：
- 长按 300ms 后进入拖拽模式（已有 `LONG_PRESS_MS` 常量）
- 拖拽时其他牌让位动画（基于 `computeCardLayouts` 实时重排）
- 释放到弃牌堆区域时触发出牌；释放到其他位置回弹原位
**依赖**：4.1（布局重构）
**风险**：高（需要精确的 pointer 事件处理 + 布局实时计算）
**文件**：`GameBoard.tsx`

### 4.3 全局过渡动画队列化 ★★★★
**问题**：当前多个动画（聚焦、出牌、摸牌、罚摸、爆牌）各自独立 setTimeout，可能叠加/冲突。例如：聚焦动画期间用户又触发了 snatch_window，动画堆叠。
**方案**：
- 在 `gameStore` 新增 `animationQueue`，所有动画注册到队列
- 每个动画声明 `duration` + `blocking`（是否阻塞后续动画）
- 非阻塞动画可并行，阻塞动画串行
- 提供 `skipAnimation()` API 供用户跳过
**依赖**：3.1、3.2、3.3、3.5
**风险**：高（重构所有动画触发点）
**文件**：`gameStore.ts`、`GameBoard.tsx`

### 4.4 客户端预测出牌（乐观更新） ★★★★
**问题**：当前所有动作都等服务端 `statePatch` 回来才更新 UI，网络延迟高时点击到看到反馈有 100~300ms 延迟。
**方案**：
- 玩家点击出牌后，立即从 `hand` 移除该牌、插入弃牌堆（乐观更新）
- 同时发送 `playCard` 事件
- 收到 `statePatch` 后核对，不一致则回滚
**依赖**：3.1（出牌飞行动画）—可让动画更流畅
**风险**：高（需处理回滚、撤销栈、状态不一致情况）
**文件**：`gameStore.ts`、`GameBoard.tsx`

### 4.5 音效系统统一化 ★★★★
**问题**：`audio.ts` 是命令式 API，散落在 `gameStore` 各处调用，难维护。
**方案**：
- 抽象 `AudioEventBus`，所有音效通过事件触发（`emit("card.play", { color })`）
- 音效资源预加载 + 懒加载策略
- 音量、静音、BGM 切换统一管理
- 支持音频池（同一音效快速触发时不打断）
**依赖**：无
**风险**：中（重构 audio 模块，不涉及游戏逻辑）
**文件**：`audio.ts`、`gameStore.ts`

---

## P5 — 最高难度（架构级改动）

### 5.1 状态机化 gameStore ★★★★★
**问题**：`gameStore` 的 `statePatch` handler 是一个巨大的 switch case，每个 case 里又嵌套 if-else，可读性差，新功能加入容易遗漏边界。
**方案**：
- 用 XState 或自实现的有限状态机建模游戏阶段：`lobby → character_draft → in_game.main → in_game.snatch_window → in_game.post_draw_window → game_over`
- 每个状态有明确的进入/退出钩子
- 动画触发与状态转换解耦
**依赖**：4.3（动画队列化）
**风险**：极高（涉及所有状态流转逻辑）
**文件**：`gameStore.ts`（重构）、新增 `fsm/` 目录

### 5.2 Web Worker 游戏逻辑计算 ★★★★★
**问题**：所有动画 + React 渲染 + 状态计算都在主线程，复杂场景（6人 + 大量手牌 + 多动画）可能掉帧。
**方案**：
- 将 `computeCardLayouts`、动画插值计算、手牌排序等计算密集任务移到 Web Worker
- 主线程仅负责 DOM 更新和事件分发
**依赖**：4.3（动画队列化）
**风险**：极高（需重构数据流，调试困难）
**文件**：新增 `workers/` 目录

### 5.3 可访问性（a11y）改造 ★★★★★
**问题**：当前所有交互仅靠鼠标 hover/click，无键盘导航；无 ARIA 属性；无屏幕阅读器支持。
**方案**：
- 所有可交互元素加 `tabIndex` + `role` + `aria-label`
- 键盘快捷键：1-9 选手牌、Space 摸牌、Enter 出牌、U 喊UNO
- 焦点环（focus ring）可视化
**依赖**：无
**风险**：中（工作量巨大但不涉及游戏逻辑）
**文件**：所有组件

---

## 补充建议（用户未提及但值得考虑）

### A. 卡牌设计优化
- 当前卡牌仅用纯色 + 数字 + 角标，辨识度一般。建议：
  - 数字牌加图案（如 7=星星，9=三角形），辅助色盲玩家
  - wild 牌加彩虹渐变，wild_draw_four 加黑色斜杠
  - 功能牌加图标：skip=禁止符号，reverse=循环箭头，draw_two=+2
- 难度：★★（纯视觉设计）

### B. 对局内信息透明化
- 显示"当前队伍手牌总数"对比条（teamA vs teamB），让玩家感知整体局势
- 显示"已出牌数 / 总牌数"统计
- 难度：★★（需新增 UI 组件）

### C. 操作撤销 / 确认机制
- 出牌前 0.5s 显示"确认出牌？"浮层（可选开关）
- 误触保护：出 wild 牌后选色前可取消
- 难度：★★★

### D. 移动端横屏适配
- 当前 `@media (max-width: 950px) and (orientation: landscape)` 已处理横屏，但竖屏下完全不可用
- 建议竖屏下显示"请横屏游戏"提示，或做一套竖屏布局
- 难度：★★★

### E. 观战视角优化
- 当前观战者只能看一个视角，建议加"跟随某玩家"模式，或"上帝视角"（看所有人手牌）
- 难度：★★★

### F. 网络状态指示
- 右上角显示 ping 值 + 连接状态
- 网络波动时 toast 提示
- 难度：★★

### G. 对局回放
- 服务端记录所有动作序列，客户端可"回放上一局"
- 难度：★★★★★（需服务端配合）

---

## 实施顺序建议

1. **第一批（P1 全部）**：低风险快速见效，1-2 天完成
2. **第二批（P2 选中 2.1, 2.2, 2.4）**：核心交互体验提升
3. **第三批（P3 选中 3.1, 3.3, 3.5）**：视觉冲击力大幅提升
4. **第四批（4.1 布局重构 + 4.5 音效系统）**：技术债清理 + 为后续动画铺路
5. **第五批（4.3 动画队列 + 4.4 客户端预测）**：性能与响应性质变
6. **P5 项目**：视实际需要决定是否启动，优先级最低

> **注**：P4/P5 项目都建议单独开分支 + 充分测试，避免破坏现有可用功能。
