# 演出系统设计文档

> 前身为"音频系统"，现升级为事件驱动的广义媒体演出层（Presentation Layer）。  
> BGM 部分不变，由原有 `AudioController` 继续管理。  
> **2024-07 更新**：职责边界重新划分——常驻背景图移至 CSS 管理，演出系统只负责一次性弹出媒体。

---

## 一、概述与职责边界

| 系统 | 职责 | 文件 | 生命周期 |
|------|------|------|---------|
| **AudioController** | 持续播放 BGM，随场景/优劣势平滑切换 | `audio.ts` | 常驻 |
| **PresentationSystem** | 事件触发的一次性媒体演出：音效 + GIF动图 + 视频片段 | `presentation/` | 弹出后自动消失 |
| **CSS 背景** | 各场景常驻背景图片（开屏/大厅/房间/对局/结算） | `styles.css` + `public/images/backgrounds/` | 跟随页面切换 |

**关键区别：**
- BGM / CSS 背景 = 常驻显示，调用者只关心"当前是什么场景"
- 演出 = 一次性弹出，由游戏事件触发，有时长、有优先级、有视觉层，播放完自动消失

**为什么结算画面用 CSS 而不是演出系统？**  
结算弹窗需要常驻显示直到用户操作（再来一局/退出），且上面有交互按钮。演出系统是"弹出→消失"模式，不适合承载交互 UI。

---

## 二、音频资源现状

### BGM（`/public/audio/bgm/`，15 首）

| 文件 | 场景 | 调用状态 |
|------|------|---------|
| `login_lobby.mp3` | 登录/大厅 | ✅ 已接入（`syncViewBgm`） |
| `room_waiting.mp3` | 等待房间 | ✅ 已接入 |
| `game_base.mp3` | 游戏基础 | ✅ 已接入（`startGameBgmCycle`） |
| `game_advantage_1~5.mp3` | 己方优势时（随机选一首） | ✅ 已接入（`syncGameBgm`） |
| `game_disadvantage_1~5.mp3` | 己方劣势时（随机选一首） | ✅ 已接入（`syncGameBgm`） |
| `result_win.mp3` | 胜利结算 | ✅ 已接入（`playResultBgm`） |
| `result_lose.mp3` | 失败结算 | ✅ 已接入（`playResultBgm`） |

### SFX（`/public/audio/sfx/`，3 个活跃 + 3 个预留）

| 文件 | 注册为 | 触发时机 | 调用状态 |
|------|--------|---------|---------|
| `reverse_igiari.mp3` | `card.reverse` | 打出反转牌 | ✅ 已接入 |
| `draw_stack_kurae.mp3` | `card.draw_two` / `card.wild_draw_four` | 打出 +2 / +4 | ✅ 已接入 |
| `skip_matta.mp3` | `card.skip` | 打出跳过牌 | ✅ 已接入 |
| `龙之介在这.mp3` | （角色系统预留） | 成步堂「天降神判」 | ⏸️ 角色系统关闭，暂未注册 |
| `龙之介是.mp3` | （角色系统预留） | 预留 | ⏸️ 角色系统关闭 |
| `龙之介桥豆.mp3` | （角色系统预留） | 预留 | ⏸️ 角色系统关闭 |

---

## 三、场景背景图（CSS 管理）

### 设计原则

- 背景图通过 CSS `background-image` 设置，不经过演出系统
- 每个背景图上方叠加半透明渐变层，保证无图时也不难看
- 图片放到 `public/images/backgrounds/` 即可，无需改代码

### 当前配置

| CSS 选择器 | 场景 | 图片路径 | 状态 |
|-----------|------|---------|------|
| `.title-view` | 开屏/标题 | `/images/backgrounds/title.jpg` | ⏳ 素材待放入 |
| `.center-view` | 登录/大厅/房间等待 | `/images/backgrounds/lobby.jpg` | ⏳ 素材待放入 |
| `.game-view` | 游戏对局 | `/images/backgrounds/game.jpg` | ⏳ 素材待放入 |
| `.settlement-overlay.result-win` | 胜利结算 | `/images/backgrounds/result_win.jpg` | ⏳ 素材待放入 |
| `.settlement-overlay.result-lose` | 失败结算 | `/images/backgrounds/result_lose.jpg` | ⏳ 素材待放入 |

### CSS 写法示例

```css
.title-view {
  background:
    linear-gradient(to bottom, rgba(0,0,0,0.3), rgba(0,0,0,0.5)),
    url(/images/backgrounds/title.jpg) center/cover no-repeat;
  background-color: #173315;   /* fallback 底色 */
}
```

图片不存在时显示 `background-color` 渐变底色；图片放入后自动生效。

---

## 四、演出系统架构

### 核心类型

```typescript
// presentation/types.ts

type PresentationPosition = "fullscreen" | "center" | "corner";

interface AudioClip {
  src: string;
  volume?: number;       // 叠加到全局音量，0~1，默认 0.78
}

interface VisualClip {
  type: "gif" | "video";
  src: string;
  position: PresentationPosition;
  width?: number;
  height?: number;
  loop?: boolean;
}

interface PresentationConfig {
  id: string;
  audio?: AudioClip;
  visual?: VisualClip;
  durationMs?: number;   // 不填则跟随音频/视频自然时长
  priority: number;      // 数字越大越重要
  blockInput?: boolean;  // true 时屏蔽操作（开局视频用）
}
```

### 优先级参考

| 优先级 | 场景 |
|--------|------|
| `0` | 普通牌效音效（reverse / skip / +2+4） |
| `1` | 角色技能发动（角色系统关闭时暂不使用） |
| `10` | 开局动画（`blockInput: true`） |

> 注：原优先级 `5`（游戏结束结算动画）已移除，结算改用 CSS 背景图。

### 当前演出注册表

```typescript
// presentation/registry.ts
const PRESENTATION_REGISTRY: Record<string, PresentationConfig> = {
  // ── 牌效音效（客户端自行推断触发）──
  "card.reverse": {
    id: "card.reverse",
    audio: { src: "/audio/sfx/reverse_igiari.mp3", volume: 0.78 },
    // visual: { type: "gif", src: "/animations/card_reverse.gif", position: "corner" },
    priority: 0
  },
  "card.skip": {
    id: "card.skip",
    audio: { src: "/audio/sfx/skip_matta.mp3", volume: 0.78 },
    // visual: { type: "gif", src: "/animations/card_skip.gif", position: "corner" },
    priority: 0
  },
  "card.draw_two": {
    id: "card.draw_two",
    audio: { src: "/audio/sfx/draw_stack_kurae.mp3", volume: 0.78 },
    // visual: { type: "gif", src: "/animations/card_draw_two.gif", position: "corner" },
    priority: 0
  },
  "card.wild_draw_four": {
    id: "card.wild_draw_four",
    audio: { src: "/audio/sfx/draw_stack_kurae.mp3", volume: 0.78 },
    // visual: { type: "gif", src: "/animations/card_wild_draw_four.gif", position: "corner" },
    priority: 0
  },

  // ── 游戏流程（gameStore 直接触发）──
  "game.intro": {
    id: "game.intro",
    // 方案A：有声视频（推荐）
    // visual: { type: "video", src: "/video/game_intro.mp4", position: "fullscreen" },
    // 方案B：GIF + 独立音效（二选一）
    // visual: { type: "gif", src: "/animations/game_intro.gif", position: "center" },
    // audio: { src: "/audio/sfx/game_start.mp3", volume: 0.85 },
    priority: 10,
    blockInput: true,
    durationMs: 0
  }
};
```

> `visual` / `audio` 注释行 = 素材占位。素材就绪后取消注释即可生效，无需改其他代码。  
> `durationMs: 0` + 无 audio/visual → store 自动跳过，不影响游戏流程。

---

## 五、触发机制

### 客户端自行推断（牌效）

`gameStore.ts` 在 `statePatch` 里检测弃牌堆顶牌变化：

```
topCard.id 改变 → triggerPresentation("card." + topCard.kind)
```

无需服务端配合，纯前端推断。

### 服务端 hint（技能、特殊事件）

`StatePatchEvent`（通过 `GameViewEventBase`）新增可选字段：

```typescript
presentationHint?: string;   // e.g. "skill.naruhodou.judgment"
```

服务端在 skill 发动成功后，把 `skill.presentationId` 附带进广播。  
客户端收到后调用 `triggerPresentation(event.presentationHint)`。

> 当前角色系统已关闭，`presentationHint` 机制保留但暂不使用。

### 游戏生命周期事件

| 事件 | 触发演出 | 在哪触发 |
|------|---------|---------|
| `gameStart` | `"game.intro"` | `gameStore.ts` |
| `gameOver`（赢） | `playResultBgm(true)` + CSS 背景切换 | `gameStore.ts` + `GameBoard.tsx` |
| `gameOver`（输） | `playResultBgm(false)` + CSS 背景切换 | `gameStore.ts` + `GameBoard.tsx` |

> 结算不再触发演出系统（`triggerPresentation("game.result.*")` 已移除），  
> 而是通过 `.settlement-overlay.result-win / .result-lose` CSS 类切换背景图。

---

## 六、队列与优先级规则

1. 当前无演出 → 直接播放
2. 新演出 > 当前优先级 → 中断当前，立即播放
3. 新演出 = 优先级 0（牌效）→ 丢弃队列中已有的优先级 0 条目，加入一条新的
4. 其他情况 → 追加到队列末尾
5. 当前演出结束（超时 or 视频 `onEnded`）→ 取队列下一条

---

## 七、文件结构

```
apps/client/src/
  presentation/
    types.ts              ← PresentationConfig 等类型
    registry.ts           ← PRESENTATION_REGISTRY 注册表
    store.ts              ← Zustand slice（current / queue / trigger / dismiss）
  components/
    PresentationOverlay.tsx  ← React 渲染层（固定 z-index 叠在游戏上方）
  styles.css                ← 场景背景图（background-image）

apps/client/public/
  audio/
    bgm/                  ← BGM（15 首，全部已接入）
    sfx/                  ← SFX（3 活跃 + 3 角色预留）
  images/
    backgrounds/          ← 场景背景图（title / lobby / game / result_win / result_lose）
  animations/             ← 演出 GIF 动图（牌效 GIF / 开局 GIF）
  video/                  ← 演出视频（开局 .mp4，与 GIF 二选一）
```

---

## 八、角色技能与演出的整合（预留）

角色系统当前已关闭。以下为未来重新启用时的参考：

角色定义 `SkillDefinition` / `SkillPublicInfo` 新增 `presentationId?: string`。  
服务端 skill handler 在 `onActivate` 成功后，把 `skill.presentationId` 放进 `presentationHint` 广播。

**开发新角色时增加演出只需三步：**
1. 把音效/视频/GIF 放到对应 `public/` 子目录
2. 在 `presentation/registry.ts` 加一条记录
3. 在角色定义里写 `presentationId: "skill.xxx.yyy"`

---

## 九、素材待办清单

| 编号 | 素材 | 放置路径 | 用途 |
|------|------|---------|------|
| 1 | 开屏背景图 | `public/images/backgrounds/title.jpg` | `.title-view` 背景 |
| 2 | 大厅/房间背景图 | `public/images/backgrounds/lobby.jpg` | `.center-view` 背景 |
| 3 | 游戏对局背景图 | `public/images/backgrounds/game.jpg` | `.game-view` 背景 |
| 4 | 胜利结算背景图 | `public/images/backgrounds/result_win.jpg` | `.settlement-overlay.result-win` 背景 |
| 5 | 失败结算背景图 | `public/images/backgrounds/result_lose.jpg` | `.settlement-overlay.result-lose` 背景 |
| 6 | Reverse 牌效 GIF | `public/animations/card_reverse.gif` | `card.reverse` 演出 visual |
| 7 | Skip 牌效 GIF | `public/animations/card_skip.gif` | `card.skip` 演出 visual |
| 8 | +2 牌效 GIF | `public/animations/card_draw_two.gif` | `card.draw_two` 演出 visual |
| 9 | +4 牌效 GIF | `public/animations/card_wild_draw_four.gif` | `card.wild_draw_four` 演出 visual |
| 10 | 游戏开始视频 | `public/video/game_intro.mp4` | `game.intro` 演出 visual（方案A） |
| 11 | 游戏开始 GIF | `public/animations/game_intro.gif` | `game.intro` 演出 visual（方案B） |
| 12 | 游戏开始音效 | `public/audio/sfx/game_start.mp3` | `game.intro` 演出 audio（方案B） |

> 素材放入对应目录后，在 `registry.ts` 中取消注释对应行即可生效。

---

## 十、已知局限与后续方向

| 问题 | 影响 | 解决方向 |
|------|------|---------|
| 所有背景图片暂无 | 场景只有渐变底色 | 待素材就绪后放入 `public/images/backgrounds/` |
| 牌效 GIF 暂无 | 牌效只有音效，无视觉 | 待素材就绪后取消 `registry.ts` 注释 |
| 开局视频/GIF 暂无 | `game.intro` 被自动跳过 | 待素材就绪后取消注释，同时设置 `durationMs` |
| 角色系统已关闭 | 3 个角色 SFX 暂不注册 | 角色系统重新启用时在 registry 加记录 |
